meta {
  name: Download Registration
  type: http
  seq: 4
}

get {
  url: {{api_baseurl}}/admin/registration/download
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
