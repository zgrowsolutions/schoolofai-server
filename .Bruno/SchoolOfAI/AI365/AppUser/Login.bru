meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{api_baseurl}}/ai365/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"pugazhonline@gmail.com",
    "password":"Password1"
  }
}

script:post-response {
  bru.setEnvVar('access_token', res.body.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
