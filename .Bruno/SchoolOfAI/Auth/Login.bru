meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{api_baseurl}}/admin/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"pugazhonline@gmail.com",
    "password":"password"
  }
}

script:post-response {
  bru.setEnvVar("access_token", res.body.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
